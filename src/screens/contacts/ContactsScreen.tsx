import { Card } from '../../components/index.ts';

export default function ContactsScreen() {
  return (
    <Card title="Contacts">
      <p style={{ color: 'var(--ink-2)' }}>Contacts and the memory board land here.</p>
    </Card>
  );
}
