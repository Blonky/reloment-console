import { Card } from '../../components/index.ts';

export default function TrustScreen() {
  return (
    <Card title="Trust & Settings">
      <p style={{ color: 'var(--ink-2)' }}>
        The kill switch, opt-out ledger, and audit trail land here.
      </p>
    </Card>
  );
}
