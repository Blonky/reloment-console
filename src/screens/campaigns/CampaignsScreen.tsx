import { Card } from '../../components/index.ts';

export default function CampaignsScreen() {
  return (
    <Card title="Campaigns">
      <p style={{ color: 'var(--ink-2)' }}>Playbook runs and exclusions land here.</p>
    </Card>
  );
}
